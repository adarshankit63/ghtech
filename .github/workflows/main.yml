name: Windows - Litemanager
on: 
workflow_dispatch:

jobs:
build:
name: Start Building-
runs-on: windows-latest
Steps:
- name: Downloading & installing Essentials
run:
   Invoke-WebRequest -Uri "https://gitlab.com/chamod12/1m_win-10_glthub_rdp/-/rav/main/Dounloads.bat* -OutFile "Downloads.bat"
    Cmd /c Downloads.bat 
    - name: connect to  Litemanager
  run: cmd /c show.bat

  - name: Time Counter
    run: cmd /c loop.bat
